export const handleUnderConstructionClick = (e) => {
    e.preventDefault();
    // Navigate to under construction page - adjust this based on your routing
    window.location.href = '/under-construction';
};